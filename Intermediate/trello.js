// ToDo application initial start.
const myTodo = [];

myTodo.push('Take Coffee');
myTodo.push('Learn JS');
myTodo.push('Play Soccer');
myTodo.push('CodeAndPlay');

for (i = myTodo.length - 1; i >= 0; i--){
    console.log(myTodo[i]);
    
}