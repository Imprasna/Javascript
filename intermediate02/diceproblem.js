// for random number
let upper = 5;
let lower = 10;

// here we use library and this how we use repeated library in a problem
let randomNum = (Math.floor(Math.random() * (upper - lower)) + upper +1);

console.log(randomNum);




// IMPORTANT RULE
// == - only checks for the values
// For object you'll have be pretty more careful in assigning the "==" & "==="
