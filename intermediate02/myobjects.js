// creating objects

// Rules
// 1. Colon is important either in Js or also in React.
// 2. Comma is super important


let youtubeChannel = {
    title: 'Baptist Academy',
    owner: 'Prasanna',
    numOfPlaylists: 20,
    videoDescription: 'For the school'
}

console.log(youtubeChannel);

// To call the specific values, "${youtubeChannel.videoDescription}" this is how it is called.
console.log(`This is the youtube channel ${youtubeChannel.videoDescription} and was created 
by ${youtubeChannel.owner} it contains ${youtubeChannel.numOfPlaylists} playlists.`);

